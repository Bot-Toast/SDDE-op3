import java.math.BigInteger;
import java.util.Random;

public class KeyGenPerfTest {

    /*
    private final static BigInteger one = new BigInteger("1");
    private final static BigInteger zero = new BigInteger("0");

    private BigInteger p, q, n, phi, d, c;
    private BigInteger e = BigInteger.valueOf(65537);
    private int bitSize = 1536;
    private Random rando = new Random();


    public  void RSAKeyGen() {
        p = BigInteger.probablePrime(bitSize, rando);
        q = BigInteger.probablePrime(bitSize, rando);
        n = p.multiply(q);
        phi = p.subtract(one).multiply(q.subtract(one));
        d = e.modInverse(phi);






}*/}




